#include <stdio.h>

    int binary_search(int arr[],int n)
    {   
        int low=arr[0];
        int high=arr[n-1];
        while (low<=high)
        {
            int mid = (low+high)/2;
        }
        
    }
    int main(){
    int arr[]={3,2,3,4,5,6};
    int n=6;

return 0;
}